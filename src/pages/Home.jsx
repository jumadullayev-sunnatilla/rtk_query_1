import axios from "axios";
import React, { useEffect, useState } from "react";

const Home = () => {
  const [data, setData] = useState(null);

  return (
    <div>
      <h2 className="text-center text-3xl">Home</h2>
    </div>
  );
};

export default Home;
